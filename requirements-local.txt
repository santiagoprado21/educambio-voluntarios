# Dependencias para ejecutar LOCALMENTE (en tu computadora)
# Incluye todo lo del backend + herramientas adicionales

# Backend (necesario en Render)
Flask==3.0.0
Flask-CORS==4.0.0
psycopg2-binary==2.9.9

# Matcher (solo para uso local)
pandas==2.1.3
openpyxl==3.1.2
requests==2.31.0

